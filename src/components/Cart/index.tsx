import React from 'react';

interface IProduct {
    id: string;
    title: string;
    category: string;
    description: string;
    price: number;
    image: string;
}

export const ProductCart: React.FC = () => {

  return (
      <>

      </>
  );
};
